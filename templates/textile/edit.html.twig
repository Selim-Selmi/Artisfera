{% extends 'base.html.twig' %}

{% block title %}Edit Textile{% endblock %}

{% block body %}

<style>
/* Général */
body {
    font-family: 'Arial', sans-serif;
    background-color: #f4f4f9;
    color: #333;
    padding: 20px;
}

/* Titre principal */
h1 {
    color: #1e130c;
    font-size: 28px;
    text-align: center;
    margin-bottom: 20px;
}

/* Formulaire */
form {
    display: flex;
    flex-direction: column;
    gap: 10px;
    background-color: #ffffff;
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    max-width: 500px;
    margin: 0 auto;
}

/* Labels */
label {
    font-weight: bold;
    color: #555;
}

/* Champs de saisie */
input[type="text"],
input[type="number"],
input[type="email"],
textarea,
select {
    width: 100%;
    padding: 10px;
    border: 2px solid #ddd;
    border-radius: 6px;
    font-size: 14px;
    transition: border-color 0.3s ease, box-shadow 0.3s ease;
}

/* Effet au survol et au focus des champs */
input:focus,
textarea:focus,
select:focus {
    border-color: #1e130c;
    box-shadow: 0 0 6px rgba(30, 19, 12, 0.5);
    outline: none;
}

/* Boutons */
.btn {
    display: inline-block;
    padding: 10px 20px;
    font-size: 14px;
    font-weight: bold;
    color: #ffffff;
    background-color: #1e130c;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    text-decoration: none;
    transition: background-color 0.3s ease;
    text-align: center;
}

.btn:hover {
    background-color: #8b5e34;
}

/* Lien retour */
a {
    display: block;
    text-align: center;
    margin-top: 20px;
    color: #1e130c;
    font-weight: bold;
    text-decoration: none;
    transition: color 0.3s ease;
}

a:hover {
    color: #8b5e34;
}
</Style>
    <h1>Edit Textile</h1>

    {{ form_start(form) }}
        {{ form_row(form.nom) }}
        {{ form_row(form.type) }}
        {{ form_row(form.description) }}
        {{ form_row(form.matiere) }}
        {{ form_row(form.couleur) }}
        {{ form_row(form.dimension) }}
        {{ form_row(form.createur) }}
        
        <div>
            <label>Current Image</label>
            <img src="{{ asset(textile.image) }}" alt="Current image" style="max-width: 200px;">
        </div>

        {{ form_row(form.imageFile) }}

        {{ form_row(form.technique) }}

        <button class="btn btn-primary">Save</button>
    {{ form_end(form) }}
        <a href="{{ path('app_textile_index') }}">back to list</a>

{% endblock %}
