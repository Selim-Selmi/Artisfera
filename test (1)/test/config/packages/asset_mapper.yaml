framework:
    asset_mapper:
        # The paths to make available to the asset mapper.
        paths:
            - assets/


# framework:
#     asset_mapper:
#         paths:
#             - '%kernel.project_dir%/public/assets'