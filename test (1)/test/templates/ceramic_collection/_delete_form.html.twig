<form method="post" action="{{ path('app_ceramic_collection_delete', {'id': ceramic_collection.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ceramic_collection.id) }}">
    <button class="btn">Delete</button>
</form>
