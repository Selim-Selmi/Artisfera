<form method="post" action="{{ path('app_collections_delete', {'id': collection_t.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ collection_t.id) }}">
    <button class="btn">Delete</button>
</form>
